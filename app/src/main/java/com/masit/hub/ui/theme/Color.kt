package com.masit.hub.ui.theme

import androidx.compose.ui.graphics.Color

// MAS IT Brand Colors
val PrimaryBlue = Color(0xFF1E1B8B)       // Dark navy blue (header, buttons)
val PrimaryBlueDark = Color(0xFF16147A)   // Darker blue variant
val AccentYellow = Color(0xFFE5A800)      // Golden yellow (CTA buttons, active icons)
val AccentYellowLight = Color(0xFFF5BD00) // Lighter yellow variant

// Status Colors
val StatusMenunggu = Color(0xFFE5A800)    // Yellow - waiting
val StatusDitangani = Color(0xFF1E1B8B)   // Blue - being handled
val StatusSelesai = Color(0xFF2E7D32)     // Green - completed
val StatusBatal = Color(0xFF9E9E9E)       // Grey - cancelled

// Background Colors
val BackgroundLight = Color(0xFFF5F3F0)   // Warm off-white background
val BackgroundCard = Color(0xFFFFFFFF)    // White card background
val SurfaceColor = Color(0xFFFFFFFF)

// Text Colors
val TextPrimary = Color(0xFF1A1A2E)       // Very dark navy for headings
val TextSecondary = Color(0xFF6B7280)     // Medium grey for subtitles
val TextHint = Color(0xFF9CA3AF)          // Light grey for placeholders
val TextOnPrimary = Color(0xFFFFFFFF)     // White text on dark bg

// Input / Border
val InputBackground = Color(0xFFFFFFFF)
val InputBorder = Color(0xFFE5E7EB)
val InputBorderFocused = Color(0xFF1E1B8B)

// Bottom Nav
val BottomNavBackground = Color(0xFF1E1B8B)

// Delete / Cancel Button
val DangerRed = Color(0xFF8B1A1A)

// Category button border
val CategoryBorder = Color(0xFFD1D5DB)